class Unit {
  constructor (pivot, cells) {
    this.pivot = pivot;
    this.cells = cells;
  }
  move (direction) {
    // TODO:
    return new Unit();
  }
  rotate (direction) {
    // TODO:
    return new Unit();
  }
}

module.exports = Unit;
